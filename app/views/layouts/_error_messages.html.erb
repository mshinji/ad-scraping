<% if model.errors.any? %>
  <div class="alert alert-danger">
    <h4><%= pluralize(model.errors.count, "error") %> </h4>
    <ul>
      <% model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
