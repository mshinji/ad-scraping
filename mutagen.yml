sync:
  app:
    mode: "two-way-resolved"
    alpha: "./"
    beta: "myapp:/home/vagrant/app"
    ignore:
      vcs: true
      paths:
        - "/node_modules"
        - "/log"
        - "/tmp"
        - "/vendor"
